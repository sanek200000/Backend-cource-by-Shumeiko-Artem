stages:
  - build
  - deploy

build-job:     
  stage: build
  script:
    - docker build -t booking-api-image .

deploy-job:
  stage: deploy
  before_script:
    - cp ${ENV} .env
    - cp ${TEST_ENV} .env-test
  #environment: production
  script:
    - docker compose up -d
